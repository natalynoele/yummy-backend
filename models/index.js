const { User } = require("./users");
const { Recipe, Category } = require("./recipes");
const {Ingredients} = require("./ingredients")
module.exports = {
  User,
  Recipe,
  Category,
  Ingredients,
};
